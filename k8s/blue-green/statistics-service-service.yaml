apiVersion: v1
kind: Service
metadata: { name: statistics-service, namespace: piggymetrics }
spec:
  selector: { app: statistics-service, color: blue }
  ports: [{ port: 7000, targetPort: 7000 }]
---
apiVersion: v1
kind: Service
metadata: { name: statistics-service-preview, namespace: piggymetrics }
spec:
  selector: { app: statistics-service, color: green }
  ports: [{ port: 7000, targetPort: 7000 }]
