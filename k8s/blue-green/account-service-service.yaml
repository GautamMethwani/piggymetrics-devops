apiVersion: v1
kind: Service
metadata: { name: account-service, namespace: piggymetrics }
spec:
  selector: { app: account-service, color: blue }
  ports: [{ port: 6000, targetPort: 6000 }]
---
apiVersion: v1
kind: Service
metadata: { name: account-service-preview, namespace: piggymetrics }
spec:
  selector: { app: account-service, color: green }
  ports: [{ port: 6000, targetPort: 6000 }]
