apiVersion: v1
kind: Service
metadata: { name: auth-service, namespace: piggymetrics }
spec:
  selector: { app: auth-service, color: blue }
  ports: [{ port: 5000, targetPort: 5000 }]
---
apiVersion: v1
kind: Service
metadata: { name: auth-service-preview, namespace: piggymetrics }
spec:
  selector: { app: auth-service, color: green }
  ports: [{ port: 5000, targetPort: 5000 }]
